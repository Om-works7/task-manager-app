import { OnHoverHighlightDirective } from './on-hover-highlight.directive';

describe('OnHoverHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
